# Set secrets in Railway Variables; never commit actual values.
APP_PASSWORD=
OPENAI_API_KEY=
DATA_DIR=/data
# Internal conservative quota, NOT a provider invoice or a total hosting cap.
AI_MONTHLY_QUOTA_USD=30
PORT=3000
